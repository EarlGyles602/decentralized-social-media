[project]
name = 'decentralized-social-media'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.content-ownership]
path = 'contracts/content-ownership.clar'
clarity_version = 3
epoch = 'latest'

[contracts.moderation-governance]
path = 'contracts/moderation-governance.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
